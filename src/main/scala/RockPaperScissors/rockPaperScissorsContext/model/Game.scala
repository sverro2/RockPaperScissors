package rockPaperScissorsContext.model

class Game(playerOne: Player, playerTwo: Player, var turns: Turn = new Turn(1)) {
  def startNew(player1: Player, player2: Player) = { }
  def playShape(playedShape: PlayedShape) = {

  }
}
